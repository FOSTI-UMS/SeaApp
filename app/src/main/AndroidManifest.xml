<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fostiums.seaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeaApp"
        tools:replace="android:icon">
        <activity
            android:theme="@style/Theme.SeaApp.NoActionBar"
            android:name=".ui.penjual.ui.tambah.PenjualTambahProduct"
            android:exported="true" />
        <activity
            android:name=".ui.penjual.PenjualActivity"
            android:label="@string/title_activity_penjual"
            android:theme="@style/Theme.SeaApp.NoActionBar" />
        <activity android:name=".ui.product.ProductPage" />
        <activity android:name=".ui.auth.Register" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.auth.Login"
            android:label="@string/app_name"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>